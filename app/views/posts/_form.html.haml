= simple_form_for ([@list,@post]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :content, input_html: { rows: 15 }
    = f.association :list, selected: @list.id

  .form-actions
    = f.button :submit